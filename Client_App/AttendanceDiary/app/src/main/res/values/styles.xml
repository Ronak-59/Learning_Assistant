<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->

    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/accentColor</item>

    </style>

    <style name="ProgressTheme" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:color">@color/material_blue_grey_950</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">20dip</item>
        <item name="android:progressDrawable">@drawable/progressbar</item>
    </style>

    <style name="ButtonTheme" parent="android:Widget.Holo.Button.Borderless">
        <item name="android:textColor">#000</item>
        <item name="android:padding">8dp</item>
    </style>

</resources>
